# Implementation status — 2026-09-05

Shade V1 is implemented and its complete functional, recovery, operational and performance acceptance passes for the current distribution. The runtime, both SDKs, compact skill and twenty-session real-daemon harness are complete. The [current evidence bundle](../artifacts/v1-release-validation.json) records `status: passed` and `final_v1_release: true`, binding the accepted source, binaries, logs and measurements by SHA-256.

## Latest implementation work

- Added reproducible local [release packaging](RELEASE.md#package-an-accepted-distribution). The packager verifies the accepted binary, recorded source/evidence digests, skill and APFS limits before producing a single-binary archive with installation instructions, manifest and checksums. The [packaging verification](../artifacts/packaging-validation.json) records nine passing tests, identical bytes across two real package builds, all 38 payload checksums and successful execution of the extracted binary's version command. The archive is 4,765,924 bytes. The accepted runtime source and binary remain identified by the existing release bundle.
- The immutable APFS inventory now reads directory entries without fetching every file's attributes. It validates pinned directories and falls back to the full inventory for hardlinks, unknown entries and trees above 25,000 entries. The boundary regression and 145 ordinary Rust tests pass, with Clippy across all targets/features. The quick benchmark now retains twenty clones and uses the release gate's deterministic payload.
- The [complete current recovery matrix](../artifacts/v1-release/crash.json) passes all 251 cases across 138 points, with 266 actual SIGKILLs. The same distribution passes fourteen real-manager cases, four optimized CLI/LaunchAgent scenarios, twelve TypeScript tests and the [twenty-session harness](../artifacts/v1-release/zenith.json). All final source and binary identities are verified in the evidence bundle.
- The [ordinary release gate](../artifacts/v1-release/apfs.json) passes every threshold: materialization p50 270.121 ms / p95 280.917 ms, context p95 47.430 ms, CLI/IPC p95 4.220 ms, common responses at most 512 bytes and private growth of 0 bytes for twenty COW workspaces versus 2,048,000,000 bytes for full copies. It uses the unchanged accepted binaries with 25,000 entries, twenty retained workspaces and 100 context/IPC samples.
- The [previous ordinary gate](../artifacts/v1-release/apfs-previous.json) failed materialization p50 at 324.872 ms. A single predeclared complete measurement after observing lower host CPU activity passed; its [launch record](../artifacts/v1-release/performance-launch.json) records load and free space without claiming exclusive host use. [Diagnostic measurements](../artifacts/apfs-inventory-investigation/README.md) retain the earlier failures and controls. They show variability without establishing its cause. Scheduling, synchronization and warm-up experiments were discarded; current publication retains both complete disk barriers. Performance acceptance applies to the recorded workload and environment.

## Previous implementation milestones

- The [complete recovery record](../artifacts/provider-recovery-validation.json) binds 251 passing cases across all 138 registered points to one instrumented binary, with 266 actual SIGKILLs. It includes 99 additional provider cases for pnpm, Bun, Python, Cargo and Go, alongside the existing npm and lifecycle coverage. All cases verify consistency after restart and appropriate final resource cleanup.
- The same Rust source passes 144 tests, Clippy across all targets/features, fourteen real-manager tests and four optimized CLI/LaunchAgent tests. Twelve TypeScript tests, typechecking and bundling pass from identical staged source with frozen dependencies. The optimized binary also passes the [twenty-session harness](../artifacts/zenith-provider-recovery.json). The final APFS performance gate remains separate.
- Fixed the ignored-dependency checkpoint failure found by the Python fork fixture: recursive exclusions cover root/nested dependencies, ignored secrets remain implicit and other secret exclusions preserve literal filenames. The regression verifies staged/working changes, deletion, untracked files and that excluded bytes never enter Git objects. [Approved scripts](SCRIPT-APPROVALS.md) now support dynamically linked Node through exact library-file reads, library-content fingerprints and empty OpenSSL configuration.
- Fixed two acceptance defects with failing regressions: the local HTTP registry now waits for complete headers on blocking accepted sockets, and the twenty-session harness recovers a CLI `CLIENT_TIMEOUT` by polling its existing GC operation. Failed CLI/installation fixtures preserve their state for diagnosis. Older timeouts without their original state retain an unconfirmed cause; the final recorded runs pass.

- Completed interruption coverage for resolved publication and startup reconciliation. The [control-recovery record](../artifacts/control-recovery-validation.json) records 31 passing group-filtered cases and 46 actual SIGKILLs: sixteen publication cuts and fifteen double-kill recovery cases. Recovery inputs come from interrupted real operations, including an actually expired harness lease; no SQLite rows are manufactured. Transaction cases inspect rollback/commit before the final restart, and publication recovery checks a single durable handoff. All cases preserve the same SQLite inode, verify appropriate predecessor/live-workspace protection and finish cleanup without duplicate recovery events.
- The same source passes 142 Rust tests and Clippy across all targets/features. The catalog now has 118 points. This milestone records a new distribution build without claiming fresh distribution-daemon or performance acceptance; ecosystem-specific dependency cuts and the final complete binary/evidence bundle remain open.

- Fixed a reproduced publish-conflict recovery bug: SIGKILL between saving the workspace state and its publication intent left a visible conflict that could lose the original publish action. The resolution state, blocked dependency state, branch/push/CAS intent and both events now commit together. The same crash regression passes; the retained conflict resolves to a single-parent squash on both local and explicitly requested remote branches.
- Added six incremental-base and five publish-conflict SIGKILL cases. The [recovery-boundary record](../artifacts/recovery-boundaries-validation.json) includes all eleven passing filtered cases against one instrumented binary, 142 Rust tests and Clippy across all targets/features. The fixture verifies binary updates, deletion, permissions, symlinks and file/directory transitions without changing the old base or agent edits. The same new release passes the [polyglot daemon](../artifacts/configuration-recovery-boundaries.json), [LaunchAgent](../artifacts/installation-recovery-boundaries.json) and [twenty-session harness](../artifacts/zenith-recovery-boundaries.json). The catalog now has 102 points; interrupted resolved-publication completion, reconciliation and ecosystem-specific paths remain before the final complete run.

- Closed the [dependency configuration audit](DEPENDENCY-CONFIGURATION.md): private JavaScript configuration, uv system-config/keyring isolation, Cargo parent/native-cache read denial with pinned rustc, and Go's native read-only graph parser rejecting external local paths. Regressions reproduced each discovery/path failure before the fix.
- The [configuration validation record](../artifacts/configuration-validation.json) binds 142 Rust tests, Clippy across all targets/features and fourteen real-manager cases to 67 source files. The same optimized binary passes the [polyglot daemon](../artifacts/configuration-polyglot.json), [Python fork](../artifacts/python-fork-configuration.json), [LaunchAgent](../artifacts/installation-configuration.json) and [twenty-session harness](../artifacts/zenith-configuration.json). Eleven TypeScript tests and typechecking retain prior evidence after byte-for-byte verification of all 22 TypeScript source/manifest files. A filtered SIGKILL test also verifies recovery of the new JavaScript identification stage. Remaining recovery coverage and final full-release evidence are still open.

- Added [durable diagnostics](DIAGNOSTICS.md) with private SQLite storage, redaction before truncation, transactional failure/outbox persistence and explicit Rust/TypeScript/CLI lookup. The same reference survives retries and daemon restarts; CLI lookup works offline. A failed diagnostic write omits the reference and retains the known operation handle.
- The [diagnostic validation record](../artifacts/diagnostics-validation.json) records 140 Rust tests (128 workspace tests plus twelve release-gate example tests), eleven TypeScript tests, typechecking and Clippy across all targets/features. Three new filtered SIGKILL cases prove rollback before commit and durable replay after commit; the catalog now has 91 points. These filtered cases do not replace the final complete crash matrix.
- The [optimized-binary LaunchAgent run](../artifacts/installation-diagnostics.json) verifies the diagnostic while installed, after KeepAlive restart and offline after unloading, along with real npm readiness and the existing installation checks. Further configuration/recovery work and final release evidence remain open.

- Added 18 real npm interruption cases covering preparation, validation, replay, approved hooks, promotion, COW replacement, receipt persistence and LRU retirement/deletion. The [complete registered matrix](../artifacts/crash-dependencies-lifecycle.json) passes 106 cases across 88 named points. Recovery now removes interrupted promotions and retirement directories; GC waits for active preparations to persist their receipts before reading protected fingerprints. The regression demonstrated and then prevented deletion of a newly promoted live layer.
- Fork preserves edited dependency forests and retargets location-dependent Python files/links to the child. The [real uv/Python fork test](../artifacts/python-fork.json) executes an edited package through its relocated entrypoint while the parent environment is unavailable, then verifies unchanged parent bytes.
- The [dependency-recovery validation record](../artifacts/dependency-recovery-validation.json) binds that recorded release to the [twenty-session npm harness](../artifacts/zenith-dependency-recovery.json), [operational installation](../artifacts/installation-dependency-recovery.json) and Python fork. It records 133 passing Rust tests, thirteen excluded manual/host tests, Clippy across all targets/features and nine passing real-manager cases. Identical TypeScript source retains the preceding typecheck/ten-test evidence. The full crash run uses its separately identified instrumented binary.
- An earlier full attempt exceeded the old cold-preparation rendezvous budget, which is now 120 seconds. Another returned an INTERNAL error during npm setup; its isolated rerun and the final complete matrix passed, but the original cause is unconfirmed. Failed fixtures now preserve their daemon diagnostics for investigation. These observations and remaining final-release limits are recorded explicitly in the validation artifact.

- Replaced the twenty-session harness's npm stand-in with installed npm/Node and an isolated registry. The [real-host run](../artifacts/zenith-host-npm.json) proves one source fetch, two successful safe install commands (fill plus offline replay), one shared artifact and independent root/nested dependency forests. Fork now preserves agent-modified JavaScript/Python dependency bytes and inherited receipts; Cargo/Go caches are revalidated.
- Added [operational installation acceptance](INSTALLATION.md). The [release-binary run](../artifacts/installation.json) installs a unique temporary LaunchAgent, opens real npm dependencies in 1,892 ms, recovers after SIGKILL in 3,139 ms with the same SQLite inode/workspace, rejects label collisions and unloads cleanly. Interactive scheduling fixes the observed Background-throttling timeout; canonical host PATH entries make tools available under launchd.
- The [runtime/installation validation record](../artifacts/runtime-install-validation.json) binds both runs to one release digest, with 132 passing Rust tests, ten TypeScript tests, typechecking and Clippy across all targets/features. Twelve manual/host acceptance tests are excluded from the ordinary Rust command; installation was run explicitly. Remaining phase coverage and final release evidence remain open.

- Implemented [explicit JavaScript script approvals](SCRIPT-APPROVALS.md), with provider/package/version/integrity decisions stored transactionally alongside operation completion and events. Refresh builds a COW successor; approval and revocation preserve existing workspaces. Both session facades and the CLI expose inspection and decisions.
- Approved hooks use pinned Node/shell, dependency order, offline execution and package-scoped writes. Hardlinked files are detached through APFS before execution, nested dependencies remain protected, and the locked graph binds pnpm aliases/peer variants independently of shortened store-directory names. Preflight rejects unsafe dependency names and graph paths.
- Development acceptance covers real npm/pnpm/Bun approvals, cache reuse and revocation; the real CLI/Rust workflow covers canonical aliases, idempotency and daemon restart. Four additional real SIGKILL cases prove atomic approval/revocation before and after commit. These filtered crash artifacts do not replace the final full matrix. The [script-approval validation record](../artifacts/script-approvals-validation.json) records 119 Rust tests, ten TypeScript tests, all nine host-manager cases, CLI acceptance and the four filtered crash cases, with source/tool/binary digests and preserved logs.

- Extended secret policy to content outside `.env*`: a bounded binary scanner checks every reachable Git tree, a required persistent CLI filter withholds rejected bytes before object creation, and checkpoints keep private working contents separate from their clean index versions. A private index snapshot is validated before creating a Git tree, including when a local command previously bypassed the required filter. Real-Git tests verify binary identity, large inputs, rejected blobs, retention after a filter bypass, and private propagation through fork/restore.
- Added private JSON/TOML key merging and exact-byte decisions for opaque/binary files. Structural, duplicate-JSON-key and binary conflicts remain reviewable before successor allocation. Baseline contents now occupy a separate directory from metadata; review freshness also tracks known files after a credential is removed.
- The [content-secret validation record](../artifacts/content-secrets-validation.json) records 113 ordinary Rust tests (ten explicit acceptance tests excluded), TypeScript typechecking, nine TypeScript tests and Clippy across all targets/features. Typechecking also passed from identical source bytes with frozen dependencies in a temporary directory after reads from the workspace directory stalled. Final V1 certification remains a separate release gate.
- The [content-secret APFS run](../artifacts/apfs-content-secrets.json) passes all thresholds with 25,000 files: materialization p50 299.043 ms / p95 342.296 ms, context p95 50.228 ms, CLI/IPC p95 7.430 ms and common responses at most 512 bytes. Its exact release binary also passes the [twenty-session harness](../artifacts/zenith-content-secrets.json), which uses a controlled npm executable and finishes with clean lifecycle resources. This milestone's evidence does not close the remaining V1 checklist.

- Added real host dependency acceptance for all six package managers using cold isolated caches, local registries (public proxy for the pinned Go module), malicious lifecycle/build/startup fixtures and corruption/missing-cache recovery. The [recorded development run](../artifacts/host-dependencies.json) passes all nine scenarios in 40.74 seconds; exact-tool receipts are recorded separately from the controlled-tool Zenith harness. That development artifact does not certify the final distribution binary.
- Enforced replay network isolation at the macOS process-tree level, including Bun. Preserved pnpm's separate policy-verification cache so its supply-chain checks remain enabled offline.
- Replaced fragile lock/config line parsing with TOML, YAML and JSONC parsers; fixed Node interpreter identity for npm/pnpm, Go version discovery, installed-only Corepack resolution and safe Python bootstrap handling.

- Added feature-gated real-process crash injection. The [lifecycle matrix](../artifacts/crash-lifecycle.json) passes 84 cases over 68 named points, including secret decisions, adopted-session secret merges, late secret discovery by GC and incremental-base clone staging. Further phase coverage remains in the checklist.
- Corrected recovery of locked temporary Git registrations before/after moving `.git`, and moved temporary Git indexes into owned repository staging.
- Made release and secret keep/discard atomic. Reviewed merges commit their decision and successor handoff together; historical lease ownership supports already-adopted children.
- Added GC discovery of late secret changes with a key-only event preview, and review resolution for released/expired leases. Immutable private review snapshots now prevent stale decisions from discarding later edits, even when key names remain unchanged.
- Froze the instrumented executable for each crash-matrix run so concurrent Cargo builds cannot replace it between cases or change the recorded digest.

- Fixed the Rust SDK timeout recovery regression test's misplaced lookup counter and the outstanding Clippy error.
- Removed a timing-dependent TypeScript recovery test configuration: the mocked transport already injects the timeout, so recovery no longer depends on a 25 ms timer resuming inside a 30 ms budget. Its actor/key and retry-count assertions remain intact.
- Batched checkout-policy blob inspection through `git cat-file --batch`, preserving all history, LFS, filter and secret checks. The regression fixture uses SHA-256 objects, binary contents and enough input/output to exercise pipe backpressure. Its 2,049 blobs now require three subprocesses rather than 2,049; the test also rejects LFS in a later batch and filters in large attributes files.
- Added a bounded APFS directory-clone path for verified immutable bases/layers. Controlled inventory precedes cloning, and mutable workspaces, trees above 25,000 entries and trees with internal hardlinks retain per-entry cloning. Regression tests cover isolation, dangling/external symlinks, executable and directory modes, hardlink topology, unsupported objects and exclusive publication.
- Kept the daemon's multithreaded Tokio runtime while giving ordinary CLI requests a current-thread runtime. Help/version responses avoid runtime initialization.
- Turned the manual APFS latency probe into an assertion of the declared p50/p95 budgets and removed temporary production profiling output.

## Earlier distribution-binary validation

The recorded distribution run passed 104 Rust tests, with the manual latency probe excluded from ordinary test runs. Formatting, Clippy with warnings denied, TypeScript typechecking, nine deterministic protocol tests and the [simulated twenty-chat harness](../artifacts/zenith-simulated.json) pass. The TypeScript timeout regression suite additionally passed twenty consecutive runs. The ordinary TypeScript command skips the real-daemon test, which was run separately against the final release binary.

The [full APFS release benchmark](../artifacts/apfs-release.json) passes on an Apple M4 Pro with macOS 27.0/APFS, using 25,000 files, twenty workspaces and 100 context/CLI samples:

| Measurement | Observed | Required |
|---|---:|---:|
| Materialization p50 | 257.653 ms | <300 ms |
| Materialization p95 | 283.568 ms | <1,000 ms |
| Context p95 | 47.743 ms | <200 ms |
| CLI + IPC p95 | 2.760 ms | <10 ms |
| Largest common response | 512 bytes | ≤512 bytes |
| Private storage growth | 0 bytes for COW vs 2,048,000,000 bytes for byte copies | ≥5× reduction |

The storage comparison uses `ATTR_CMNEXT_PRIVATESIZE`, not an estimate of global shared bytes. The artifact retains raw samples, environment and binary digests. [The preceding run](../artifacts/apfs-before-cli-runtime.json) already passed materialization but missed CLI latency; the final binary uses the smaller CLI runtime.

The [real harness](../artifacts/zenith-real.json) uses a controlled npm executable with the real daemon, Git, APFS and SQLite. It exercises one shared dependency preparation, COW isolation, checkpoints, fork, sync/adoption, publishing, SIGKILL/restart against the same SQLite file, resumed events and final cleanup. [Validation metadata](../artifacts/validation.json) ties these checks to the same release binary digest.

The official Skill measures 239 tokens with `o200k_base` (tiktoken 0.14.0), below the 350-token limit; its exact bytes, tokenizer and limit are recorded in [skill-tokens.json](../artifacts/skill-tokens.json).

## Final V1 acceptance

Every item in the [V1 checklist](V1-CHECKLIST.md) is complete. The [final bundle](../artifacts/v1-release-validation.json) verifies all 70 source files and the distribution/gate binaries, with 145 Rust tests, twelve TypeScript tests, fourteen real-manager tests, four optimized distribution scenarios, twenty real sessions and 251 recovery cases. The complete APFS gate passes on those same source and distribution identities. [Release commands](RELEASE.md) document how to reproduce acceptance.

Development validation creates and unloads only isolated acceptance LaunchAgents. The normal `com.zenith.shade` service and standard installation paths have not been installed or replaced.
